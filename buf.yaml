# buf.yaml
version: v2
modules:
  - path: .
lint:
  use:
    - DEFAULT
    - UNARY_RPC
  disallow_comment_ignores: true
  except:
    - PACKAGE_VERSION_SUFFIX

breaking:
  use:
    - WIRE_JSON
  ignore_unstable_packages: true
